type GenesisOriginals @entity {
	id: ID!
	tokens: [Token!]! @derivedFrom(field: "registry")
	tokensMinted: BigInt!
	totalPrintsMinted: BigInt!
	maxSeedSupply: BigInt!
	maxPrintSupply: BigInt!
	reserve: BigInt!
} 

type Token @entity {
 	id: ID!
	seed: BigInt!
	owner: Account!
	registry: GenesisOriginals!
	timestamp: BigInt!
	printsMinted: BigInt!
	nextPrintPrice: BigInt!
    nextBurnPrice: BigInt!
	prints: [Print!]! @derivedFrom(field: "token")

}

type Print @entity {
 	id: ID!
	token: Token!
    pricePaid: BigInt!
    royalty: Royalty!
	owner: Account
}

type Royalty @entity {
	id: ID!
	amount: BigInt!
	recipient: Account!
	print: Print!
}

type Account @entity {
	id: ID!
	prints: [Print!]! @derivedFrom(field: "owner")
	tokens: [Token!]! @derivedFrom(field: "owner")
	royalties: [Royalty!] @derivedFrom(field: "recipient")
	burnRewardAmount: BigInt!
}